# Journal — append-only working notes

<!-- RULES (load-bearing, do not remove):
     - One file per day: YYYY-MM-DD.md. Append-only during the day; never rewrite
       history. Raw and cheap — full sentences not required.
     - Every session/routine run ENDS by appending here: what was done, learned, decided,
       and anything that failed. Entry header: "## HH:MM UTC — <routine or task name>".
     - RETENTION: 30 DAYS. The weekly memory-distill skill deletes journal files older
       than 30 days after folding durable facts into long-term.md. Nothing here is
       permanent — if it must survive, it must be distilled.
     - Date-stamp and tag durable claims FACT / INFERENCE so distillation can preserve
       provenance.
     - No email/calendar-derived content or PII in this repo — see employees/PRIVACY.md.
-->

This README stays in the template; copied journals start empty except for this file.
