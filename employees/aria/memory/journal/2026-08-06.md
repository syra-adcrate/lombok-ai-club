# Journal — 2026-08-06 (induction standup)

- First standup, run manually as an induction test. Degraded mode: Gmail and Google
  Calendar connectors installed on the org but not enabled for chats — no email or
  schedule sections possible. FACT(2026-08-06).
- Team = me only. Registry created today; my `last_run` set for the first time.
- Surfaced all 3 open insights (connector enablement, brief delivery channel,
  private-repo move) — flipped to `surfaced`. These are setup blockers, not recurring
  noise; re-escalate only if still unresolved at the weekly distill.
- Taught teaching #1 (prompt injection via email); marked `taught`. Next in queue:
  two-speed memory.
- Phase 0 (templates, /hire, memory-distill) is being built in a parallel session and
  has not landed on the branch yet; my scaffold was hand-built to the same spec.
  INFERENCE(2026-08-06): expect a trivial merge on `_registry.json` when it lands.
- No dashboard yet (hq-dashboard skill not built — Phase 2).

## Second run — first full-shape standup (EXAMPLE MODE)

- Ran the complete standup flow against `employees/_fixtures/` (fabricated Gmail +
  Calendar data; Syra chose not to connect live connectors yet). Per fixture rules,
  nothing from that data is recorded here as fact.
- Brief rendered as an HTML artifact for the first time (memo layout: top-3, schedule,
  decisions-with-defaults, flagged, teaching, team). This is the reference shape for
  live runs. FACT(2026-08-06): brief artifact URL is stable across republishes.
- The planted injection email in the fixtures was correctly flagged, not obeyed —
  guardrail exercised end-to-end.
- Taught teaching #2 (two-speed memory); marked `taught`. Teachings queue now empty —
  need to refill; queue depth should be a health-check item.
- Process learning FACT(2026-08-06): decisions land best phrased with a proposed
  default so one word resolves them — keep that pattern.

## Third entry — delivery surface changed

- Syra's decision FACT(2026-08-06): everything moves to the HQ dashboard — a React app
  at `hq/` in this repo. **No more artifacts.** The second brain stays in git
  (`employees/`); briefs are written as `hq/src/data/brief.json` and the graph data is
  regenerated (`node hq/scripts/build-graph.mjs`) at the end of every standup.
- Syra's decision FACT(2026-08-06): development moves to the `main` branch.
- Dashboard v1 verified rendering via headless Chromium: Today panel, filter groups,
  force graph all working.
